<%- include('../partials/head') %>
<style>
    img{
        height:150px;
        width: 150px;
    }
</style>

<body>
    <%- include('../partials/header') %>

    <!-- Offcanvas Overlay -->
    <div class="offcanvas-overlay"></div>

    <!-- ...:::: Start Breadcrumb Section:::... -->
    <div class="breadcrumb-section breadcrumb-bg-color--golden">
        <div class="breadcrumb-wrapper">
            <div class="container">
                <div class="row">
                    <div class="col-12">
                        <h3 class="breadcrumb-title">Order Confirmation</h3>
                        <div class="breadcrumb-nav breadcrumb-nav-color--black breadcrumb-nav-hover-color--golden">
                            <nav aria-label="breadcrumb">
                                <ul>
                                    <li><a href="/">Home</a></li>
                                    <li class="active" aria-current="page">Order Confirmation</li>
                                </ul>
                            </nav>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- ...:::: End Breadcrumb Section:::... -->

    <!-- ...:::: Start Order Confirmation Section:::... -->
    <div class="order-confirmation-section">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-8">
                    <div class="order-confirmation-wrapper">
                        <div class="order-confirmation-content">
                            <div class="order-confirmation-header">
                                <i class="fa fa-check-circle" aria-hidden="true"></i>
                                <h2>Thank You!</h2>
                                <p>Your order has been successfully placed.</p>
                            </div>
                            <div class="order-details">
                                <h3>Order Details</h3>
                                <ul>
                                    <li><strong>Order Number:</strong> <%= order.orderId %></li>
                                    <li><strong>Date:</strong> <%= new Date(order.orderDate).toLocaleString('en-GB') %></li>
                                    <li><strong>Total:</strong> ₹ <%= order.totalPrice %></li>
                                    <li><strong>Payment Method: </strong><%= order.paymentMethod %></li>
                                    
                                </ul>
                            </div>
                            <% order.products.forEach(item => { %>
                             
                          
                            <div class="order-details"> <br> <br>
                                <h3>Products Details</h3>
                                <ul>
                                    <li><strong>Item Name:</strong> <%= item.name %></li>
                                    <li><img class="img img-thumbnail" src="/productsImages/<%= item.images %>" alt=""></li>
                                    <li><strong>price:</strong> <%= item.price %>x<%= item.quantity%></li>
                                   
                                    
                                    <li><strong class="text-dark fw-bold">STATUS:<%= item.status.toUpperCase() %></strong> </li>
                                </ul>
                            </div>
                            <% }) %>
                            <div class="customer-details">
                            
                                <h3>Customer Details</h3>
                                <ul>
                                    <li><strong>Name:</strong> <%= order.address.name %></li>
                                    <li><strong>Phone:</strong> <%= order.address.phone %></li>
                                    <li><strong>Address:</strong> <%= order.address.address %>,<%= order.address.city %></li>
                                </ul>
                            </div>
                            <div class="order-actions">
                                <a href="/products" class="btn btn-primary">Continue Shopping</a>
                                <a href="/ordersHistory" class="btn btn-secondary">View My Orders</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- ...:::: End Order Confirmation Section:::... -->

    <%- include('../partials/footer') %>
</body>

</html>
